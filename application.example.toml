[server]
ip = "127.0.0.1"
port = 8000
debug = false
password = "change-me-before-exposing"

[limits]
max_sessions = 10
max_text_length = 500

[plugins]
voicevox = { enabled = true, base_url = "http://127.0.0.1:50021" }
melotts_zh = { enabled = false, device = "auto", default_speed = 1.0 }
kokoro_82m = { enabled = false, language = "a", device = "auto", default_speed = 1.0 }
